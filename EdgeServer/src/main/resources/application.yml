spring:
  application:
    name: "edge-server"

eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: "http://localhost:8761/eureka"

server:
  port: 9000